subjects = [
    'Math',
    'English',
    'Science',
    'History',
    'Geography',
]

// run a for loop
for (let i = 0; i < subjects.length; i++) {
    console.log(subjects[i]);
}
// run a while loop
let i = 0;
while (i < subjects.length) {
    console.log(subjects[i]);
    i++;
}
